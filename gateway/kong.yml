_format_version: "3.0"

services:
  - name: fastapi-backend
    url: http://sme-backend:8000  # Trả về http
    routes:
      - name: backend-route
        paths:
          - /api
        strip_path: false
        plugins:
          - name: rate-limiting
            config:
              minute: 20
              hour: 200
              policy: local