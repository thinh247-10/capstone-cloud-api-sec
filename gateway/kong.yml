_format_version: "3.0"

services:
  - name: fastapi-backend
    url: http://backend:8000
    routes:
      - name: backend-route
        paths:
          - /api
        strip_path: false
        plugins:
          - name: rate-limiting
            config:
              minute: 20
              hour: 200
              policy: local